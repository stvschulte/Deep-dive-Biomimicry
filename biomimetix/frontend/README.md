# BioMimetix Frontend

Main visual interface for BioMimetix AI.

## Run

Start the backend first on `http://localhost:8000`, then run:

```bash
npm install
npm run dev
```

Open `http://127.0.0.1:5173/`.

## Configuration

Optional `.env`:

```bash
VITE_API_BASE=http://localhost:8000/api
```
